const teamCheck = (teamInput) => 
    {
        if(teamInput == "watford")
        {
        console.Log(`${teamInput} are literally the worst team right now`)
        } else
        {
        console.log(`to be fair anyteam is better than watford at the moment, ${teaminput} are above WATFORD`)
        }
    }

    teamCheck("watford")